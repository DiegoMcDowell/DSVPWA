name: CI/CD con SAST y DAST para DSVPWA

on: [push, pull_request]

jobs:
  sast:
    runs-on: self-hosted

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Instalar dependencias Python
        run: pip install bandit pylint

      - name: Analizar con Bandit
        run: bandit -r dsvpwa -f html -o bandit_report.html || true

      - name: Analizar con Pylint
        run: pylint dsvpwa/*.py || true

      - name: Ejecutar SonarQube Scanner (local)
        run: sonar-scanner || true

      - name: Subir reporte Bandit
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: bandit_report.html

  dast:
    runs-on: self-hosted
    needs: sast

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Verificar imagen Docker
        run: |
          if ! docker image inspect dsvpwa > /dev/null 2>&1; then
            echo "🛠 Construyendo imagen 'dsvpwa'..."
            docker build -t dsvpwa .
          else
            echo "✅ Imagen 'dsvpwa' ya existe."
          fi

      - name: Lanzar contenedor accesible desde localhost
        run: |
          echo "🚀 Lanzando contenedor 'vulnerable-app'..."
          docker run -d --rm --name vulnerable-app \
            -p 127.0.0.1:65413:65413 dsvpwa

      - name: Esperar que la app esté disponible
        run: |
          echo "⌛ Esperando respuesta en http://127.0.0.1:65413"
          for i in $(seq 1 15); do
            if curl -s http://127.0.0.1:65413 | grep "<!DOCTYPE" > /dev/null; then
              echo "✔ Aplicación en línea"
              exit 0
            fi
            echo "Esperando..."
            sleep 2
          done
          echo "❌ La aplicación no respondió a tiempo"
          exit 1

      - name: Ejecutar ZAP local apuntando a localhost
        run: |
          /Applications/ZAP.app/Contents/MacOS/ZAP.sh \
            -cmd -quickurl http://127.0.0.1:65413 \
            -quickout zap_report.html -quickprogress || true

      - name: Verificar reporte ZAP
        run: |
          if [ -f zap_report.html ]; then
            echo "✔ Reporte ZAP generado correctamente"
          else
            echo "❌ Reporte ZAP NO generado"
            exit 1
          fi

      - name: Subir artefacto ZAP
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: zap_report.html
